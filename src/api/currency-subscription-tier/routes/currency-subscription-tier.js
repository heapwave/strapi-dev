'use strict';

/**
 * currency-subscription-tier router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::currency-subscription-tier.currency-subscription-tier');
