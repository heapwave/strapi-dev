'use strict';

/**
 * currency-subscription-tier controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::currency-subscription-tier.currency-subscription-tier');
